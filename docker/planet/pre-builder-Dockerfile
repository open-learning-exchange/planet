FROM node:20-bookworm-slim

WORKDIR /ng-app

COPY package.json ./

RUN apt-get update \
    && apt-get install -y --no-install-recommends \
      python3 \
      build-essential \
      jq \
    && rm -rf /var/lib/apt/lists/*

RUN npm i --silent
